# 2026-07-24 工作日志

## Miora Design 能力分析与 KK Studio 复刻需求清单
- 分析了 https://miora.design/（Agentic Creative Studio with Memory）、品牌设计文档、以及 kangarooking-skills 仓库。
- 产出交付物：`Miora能力分析与需求清单.md`
- 核心结论：KK Studio 已有强底座（IntentGate→Planner→ToolRegistry→PermissionPolicy→Executor→Verification→Memory、DurableGenerationQueue 图片/视频/音频/电商批量、画布运行时、Skill 体系）。
- Miora 标志性能力缺口（P0→P2）：
  - P0：品牌 VI 专家工作流、跨会话品牌记忆(Agent Memory)、节点级图像编辑（去背/换字/扩图/矢量化/Mockup/反向Prompt）
  - P1：多模型后端路由(APIMart GPT-Image-2/混元3D/image2.0/Seedance)、设计编译器(design_reasoning→compiled_brief→prompt)、评分案例库、专家网络调度、3D 生成(混元 TokenHub 优先+腾讯云回退)
  - P2：电影感滚动官网生成器(Vite+React scroll-scrubbing)、灵感盒/资产共享库
- 技术要点：kangarooking-skills 的 multi-agent-image 设计编译器与案例库、apimart gpt-image-2 异步轮询、hy-3d-gen 双后端、scroll-promo Vite+React 均可在 KK Studio 既有队列范式上复用。
- 风险：密钥走环境变量（APIMart/TokenHub/腾讯云）；APIMart 结果有 expires_at、混元3D URL 24h 有效需立即落库；改动遵循 AGENTS.md §3 边界与 §7 验证。

## Three.js 与项目关系澄清
- 用户确认：3D 场景在 KK Studio 的核心价值 = 搭建可控 3D 模型/场景，渲染出几何、光影、透视准确的**参考图**，再喂给 AI 图像生成作为参考/控制图。
- 现状：web 端 `animated-shader-background.tsx` 真正用 Three.js 跑极光 shader 背景（仅在 demo 引用）；`GpuBackground.tsx` 名不副实，实际是 Canvas 2D。mobile 声明了 three 但未 import。
- 设计意图（用户偏好）：若未来扩展 3D，目标方向是"3D -> 渲染参考图 -> AI 创作"，不是做游戏或纯装饰。

## Provider Connection 完整切流 — Slice A 完成

- 完成 `provider-connection-cutover-plan.md` 中 Slice A（全 Provider legacy route → Connection 映射）实施。
- **变更文件**：
  - `services/api/lib/capability-graph/providerConnectionLegacyRouteAdapter.js`：新增 CANONICAL_PROVIDER_PREFIX_TO_ID（16 provider 全映射）、PROVIDER_ID_TO_LEGACY_PREFIX 反向映射（exclude custom/systemproxy）、FORMAT_BY_PROTOCOL；新函数 resolveProviderIdFromLegacyRoute() / supportsNewLookup() 带 `-` 边界检查；selectCandidate() 扩展多 connection 歧义选择（latest verifiedAt + warning）；projectLegacyRoute() 动态 protocol 映射；SQL 新增 verified_at 列；导出内部函数。
  - `tests/unit/provider-connection-canonical-mapping.test.ts`（新增 16 test，含 prefix boundary enforcement 和 supportsNewLookup boundary）
  - `tests/unit/provider-connection-dual-read.test.ts`（更新 test 1498：多 Google connection 选 latest verified 替代 null fallback）
- **验证**：26/26 tests pass（canonical 16 + dual-read 10）；full unit 2152 pass（仅预存 flaky test 683 cancelledByParent）；typecheck pass；build ✓
- **修复的 Bug**：
  - 前缀匹配缺少边界检查：`wuyin` 误匹 `wuyinkeji-google-omni-1015-1` → 要求前缀后为 `-` 或结束
  - custom/systemproxy 错误出现在 PROVIDER_ID_TO_LEGACY_PREFIX → 排除（它们无 legacy route 前缀）
  - decryptSelectedSecret 未导出 → 加入 module.exports
  - TypeScript strict 类型错误 → 修复 typings

## 收口迭代 5 步流程（#203 / #204，第 1-2 步已完成）
- **#203 Provider Execution Dual-Read 收口**（已提交 `10219e3c`）：串行重跑 unit(2140/0/2sk)、typecheck、integration(14)、contract(15)、e2e(11)、build、desktop-smoke、ai-takeover-smoke、architecture、governance 全绿。途中两个环境拦截（非代码回归）：vite safe-delete 拦截 dist 旧产物→清空 dist 重 build 通过；3000 端口被第三方"片源库"进程占用致 desktop smoke 降级误测→taskkill PID 2664 后重跑通过。`git diff --check` 干净，任务统计 160/99/61 不变。profile.js 10 个 Provider lookup 通过新建 `profileRouteResolver.js` 接入 owner-aware dual-read，baseline 1793→1786。
- **#204 OpenSpec 文档事实漂移修复**（已提交 `d2d5beb0`）：统一 proposal/design/tasks 三份 Status 行（bounded replan executor complete、dual-read 覆盖 generation/dispatcher/profile execution），保留未完成项（全 Provider 映射/新写入切流/semantic replay/跨设备执行接管）。新增 `scripts/governance/check-openspec-status-consistency.mjs` 并接入 `governance:check` 链，以 tasks.md 为源真值逐字节比对，负向测试确认能捕获漂移。
- **第 3-5 步阻塞**：Phase 2a 外部门禁需真实 PostgreSQL（migration 001→019 演练、018 数据保留、灰度）+ 真实浏览器（关闭/重登/跨设备 SSE/Job 投影）；本机无 psql/Docker/WSL，需受控数据库环境或 VPS 演练授权。第 4 步（Provider 完整切流）依赖第 3 步，第 5 步（视频/音频 Worker）依赖第 4 步。明确约束：admission 与 execution flag 分离、回滚先关 admission 保 execution 至 lease drain、不得删 migration 018/019 数据、本地 Fake 不能代替真实证据。
- **#205 Phase 2a 外部门禁准备**（已提交 `59d730ee`，commit message 被 agents:commit 误标记为 #204）：新增 `scripts/ops/postgres/rehearse-migration-001-019.sh`（三种模式 fresh/repeat/populated，自动结构验证+JSON 报告）；新增 `docs/governance/phase-2a-gating-checklist.md`（6 大验证域：migration 演练、灰度放量 off→internal→invited→full→off、业务指标 submit/poll/cancel/退款/重复结算、真实浏览器验证、1K 媒体 benchmark、回滚协议）；package.json 新增 `db:rehearse-migrations` 系列 npm 快捷入口。演练脚本内置数据库名安全检查（拒绝 production/prod/live/master）。待环境就绪后直接执行。

