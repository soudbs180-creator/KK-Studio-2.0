# Supabase Baseline（历史索引）

Status: reference 这是旧托管数据库时代的背景记录，不是 KK Studio 当前
Schema、认证、Provider 或计费的来源。当前事实以 `services/api/`、
`infrastructure/database/migrations/`、共享 DTO 和治理文档为准。

为了防止误用，本文件不保留项目 ref、域名、密钥、RPC、RLS 或可执行 SQL。
