# Supabase CLI 资料（历史索引）

Status: reference Supabase CLI 不属于当前 KK Studio 发布链路。当前数据库
迁移入口是 `infrastructure/database/migrations/`，发布与验证入口是 `docs/setup/GUIDE.md`。

本文件不保留登录命令、项目引用、环境变量或访问令牌示例，以免旧托管
数据库被误当成当前事实。历史迁移只可在隔离环境中阅读，不能重新接入
Web、API Client 或 Agent 工具。
