# KK Studio v1.4.0 Restoration Plan

> Archived historical plan. Do not use this document as current implementation guidance for KK Studio v1.5.6.
> Current runtime facts are defined by `AGENTS.md`, `config/release-manifest.json`, and `docs/governance/PROJECT_STATE_AND_VALIDATION.md`.

This document records the safe restoration strategy for the unfinished `1.4.0` migration captured in `stash@{0}`.

## Current state

- The repository has been safely rebased to the `1.4.0` version baseline.
- Build metadata now emits `version`, `releaseDate`, and `releaseNotes` through `dist/app-version.json`.
- Architecture boundary checks have been upgraded and currently pass with explicit allowlisted migration debt.
- Full verification is currently green: `typecheck`, `architecture:check`, and `build` all pass.

## Guiding principle

Do not restore `stash@{0}` wholesale.

Restore `1.4.0` in batches:

1. Version and release metadata
2. Architecture rules and migration constraints
3. Shared contracts and compatibility layers
4. API module migration
5. Payment-sidecar migration
6. Frontend interaction and animation refinements

## Batch 1: Completed

- Version baseline restored to `1.4.0`
- Build manifest upgraded with release metadata
- Project structure document restored
- Import boundary checks upgraded
- Frontend Supabase boundary checks upgraded with explicit transitional allowlists

## Batch 1 checks

- `npm run build`
- `npm run architecture:check`
- `npm run typecheck`

Results:

- `typecheck`: passing
- `build`: passing
- `architecture:check`: passing with allowlisted migration debt

## Transitional migration debt

### Cross-app import

- `apps/payment-sidecar/src/server.ts -> apps/api/src/lib/request-authenticator.ts`

Target state:

- Move the shared authentication helper into `packages/shared`

### Frontend direct Supabase access

Historical note:

- This debt has been closed on the current branch.
- Billing, user API, admin, and secure-storage browser flows now go through typed authenticated APIs instead of direct table and RPC access.
- `npm run architecture:check` currently reports no frontend Supabase boundary exceptions.

## Batch 2: In progress

Focus:

- additive `packages/contracts` recovery only
- no runtime routing or module entrypoint rewiring

Completed so far:

- restored billing summary and credit exchange rate DTOs
- restored generation-task billing and secure-proxy DTOs
- restored `ProviderProtocolFamily` contract type needed by the new generation DTOs
- restored payment-sidecar health metadata and legacy Alipay callback compatibility path

Why this batch is safe:

- changes are contract-only and additive
- runtime changes are limited to observability and callback compatibility only
- missing `apps/api` migration files are still intentionally untouched

## Recommended next batches

### Batch 3: Shared compatibility and payment-sidecar observability

Focus:

- selective `packages/shared` compatibility shims
- compatibility wrappers in `src/services/api/*`
- `apps/payment-sidecar/src/server.ts` health and callback compatibility slices

Goal:

- improve migration readiness without rewiring the main API composition root

Checks:

- `npm run architecture:check`
- targeted unit tests for contract/client files
- payment health endpoint smoke checks

### Batch 4: API modularization

Focus:

- `apps/api/src/modules/auth/*`
- `apps/api/src/modules/billing/*`
- `apps/api/src/modules/model-catalog/*`
- `apps/api/src/modules/generation/*`

Goal:

- Continue moving business behavior behind module indexes and stable contracts

Checks:

- `npm run architecture:check`
- `npm run test:unit`
- `npm run test:contract`

### Batch 5: Payment-sidecar migration

Focus:

- `apps/payment-sidecar/src/server.ts`
- `payment-server/index.js`
- `payment-server/webhook.js`
- `src/services/api/paymentSidecarClient.ts`

Goal:

- Eliminate dual-stack drift between `payment-server` and `apps/payment-sidecar`

Checks:

- payment smoke tests
- local callback flow verification

### Batch 6: Frontend refinement recovery

Focus:

- `src/components/canvas/InfiniteCanvas.tsx`
- `src/index.css`
- `src/components/mobile/MobileHeader.tsx`
- `src/components/image/ImageCard2.tsx`
- `src/components/MobileChatFeed.tsx`
- carefully selected slices from `src/components/layout/PromptBar.tsx`

Goal:

- Restore visible `1.4.0` quality improvements without destabilizing the current application shell

Checks:

- mobile manual verification
- canvas zoom/selection verification
- prompt/model selection regression checks

## Do not restore yet

- `src/App.tsx` as a whole
- `src/components/layout/PromptBar.tsx` as a whole
- `scripts/create-portable-release.mjs` from `stash@{0}`
- `scripts/portable-launch.ps1` from `stash@{0}`
- `scripts/portable-app-server.cjs` from `stash@{0}`

Reason:

- These changes depend on missing files or high-coupling migration branches and would likely break the current runtime.
