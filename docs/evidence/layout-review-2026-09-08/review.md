# 用户布局反馈修复验收

2026-09-08。状态Prototype，整体Repairing。工程 D:\kk-studio-next，分支codex/figma-new-pages，HEAD9e20941，保留既有未提交工作。

## 本次修复

| 问题 | 修复与实际证据 |
| --- | --- |
| 390px顶部四组控件相互覆盖 | CanvasHud按实际可用宽度换行；最终5个截图宽度交叠0，回归额外覆盖801px |
| 桌面聊天开启时底栏偏约9–11px | 顶/底工具共享useCanvasViewport内容宽度，聊天与侧栏开关后重新居中；按内容边界误差<1px |
| 50%/200%加号上下偏移+7/-14px，间距变4/16px | 世界坐标锚点+屏幕坐标偏移，中心误差0、间距固定8px |
| 44px窄屏删线按钮偏移 | 根据真实尺寸居中，缩放前后中心误差0 |
| 工具栏尺寸挤压、收起丢失键盘焦点 | 原稿266×50固定槽位、aria-expanded和回焦，140ms淡入取代宽度插值；减弱动态动画0 |
| 导航内部累积1px漂移 | 47/6/20/20/55px轨道，四控件几何差值0 |

来源索引：../../reference/layout-review-2026-09-07/INDEX.md。Figma设计和动效当次均读取成功（motion nodes=[]）。窄屏换行、响应式中心与动效为工程补充。原稿200%菜单打开态和当前同状态均97×140@(0,28)，底栏6槽与导航4控件位置/尺寸差值0；不是全界面像素一致的结论。

## 验证记录

- before.json、before-*.png：原始失败几何；red.log：新增3个回归全部先失败。
- targeted.log：初次修复12过2失败，定位为旧高优先级导航偏移规则叠加新布局；删除重复偏移后targeted-final.log中14项通过。
- verify.log：首轮全量57过2失败。底栏旧固定坐标719与新中心709冲突；示范加号43.9997559px浮点矩形与严格≥44冲突。保留原稿尺寸检查、改验真实可用区中心；同时检查CSS44px和实际屏幕宽度误差<0.005px。assertions-corrected.log的2项均通过。
- verify-final.log / browser-results.json：Node24完整verify exit0，14核心+59 Edge、0跳过/flaky；类型、UI门禁、格式、构建通过。无后续实现变化，不重复全量。
- final.json / final-*.png：10种实际状态、页面错误0，390/1920已目视检查。source-compare.cjs/json、navigation-open-200.png、toolbar-desktop.png：原稿几何与同状态视觉核对。旧capture按边框外缘度量显示桌面0.5px，而实现/回归按内容区度量；两者口径区别保留。

## 自审与边界

完整性：只覆盖本表，剩余模块逐项保留。正确性：屏幕/世界坐标分别处理，未改卡片逻辑位置。健壮性：移动视口、比例、面板开关、键盘、減弱动态有真实浏览器证据。结构：HUD抽取复用，所有组件不超过300行。性能：复用已有ResizeObserver，尺寸未变不更新状态，无轮询；本批未做大画布性能验收。安全：未新增请求、后端、发布或数据清理。证据：失败、修复及来源分开保存。方案反思：取消各断点复制聊天宽度，避免后续新控件再争抢绝对坐标；不把原稿不同功能的40/20/30px按钮强制拉成同宽。

底栏字形仍为遗留Lucide，待用已缓存Figma资产替换；中文字体回退、参数与生成展示、触屏实际体验未宣称完成。初始窄屏画布为100%原始卡片坐标，可使用缩放/适应视图；未改变用户草稿或节点位置。设置页上一交付另见settings-review-2026-09-07，不混用验收计数。

下一步先查PROGRESS与fingerprints.json，沿底栏字形/卡片参数来源做下一个独立小交付，随后资产页。此处无用户操作阻塞，自动化仍有后续可执行工作。
