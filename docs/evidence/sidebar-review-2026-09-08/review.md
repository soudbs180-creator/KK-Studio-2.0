# 侧栏小交付 · 2026-09-08

状态：本单元 Prototype，整体 Repairing。演进为 extend 既有前端纠偏；不重建导航，不接后端，不发布。

## 来源与范围

- Figma 0nU0A7pq6eyjwfwm1TtWkO，170:8473：读取当日成功缓存的完整设计上下文、同状态PNG及关键帧结果，7枚sidebar资产为此前已下载的原始SVG，本轮接入使用。
- 收起边界来自layout-review-2026-09-07/workspace-design-context.json中170:8245：内框x61/y45；39×48导航x12/y140、192、244、296；搜索/切换/头像与偏置设置按170:8473。89px是控制组宽度，不是侧栏宽度。
- 本轮实时get_design_context返回UNAUTHORIZED/oauth_token_invalid_grant，原文reference/sidebar-review-2026-09-08/context-current.json。没有在条件不变时重试其它Figma通道。缓存motion为nodes=[]；prototype-read仅包含底栏等节点，没有侧栏。侧栏reactions和时序待实时连接可用后补读，不声称无原型动效。

## 实际文件

Sidebar.tsx、useSidebarLayout.ts、sidebar.css、App.tsx接入共享切换状态；workspace.css删除旧收起/窄屏隐藏规则，motion.css删除宽度插值；ui-tokens.css记录颜色/尺寸，config/ui-legacy-baseline.json减少6个Lucide符号与4项颜色用法。tests/browser/sidebar.spec.ts新增6项真实浏览器回归。UI-ALIGNMENT、UI-STANDARDS及PROGRESS同步。既有demo资源、其它会话实现、全部未提交工作保留。

## 验证和截图

- Node24.20.0，verify-final.log exit0：类型、14核心测试、ui:check63文件零违规、格式、构建、74 Edge全部通过；browser-results.json为最终报告，无失败、跳过或flaky。
- targeted.log保留首轮6过1失败：动态名称从“展开”变成“收起”后，测试旧名称定位消失。改用同一持久控件定位，保留aria状态、焦点与功能断言。targeted-final.log记录7项通过；随后新增断点焦点与粗指针检查并纳入最终74项。
- before-*是改前4视口；after-*为首轮实现截图。目视发现390偏置设置压住底栏添加，已修改窄屏位置并加真实矩形碰撞检查。final-*是修复后4视口1920/1440/768/390收起与展开、深色/浅色/粗指针截图，已查看1920收起/展开与390各态。
- source-compare.json：桌面收起宽度、四行导航、搜索、切换、头像和设置的坐标/尺寸差值全部0。只覆盖表中几何，不是整张画板像素一致声明。主画布外框宽度仍比来源少1px，属于既有右侧11px留白，本轮未扩展到整个画布布局。
- final.json与extra-review.json：页面错误0，四视口无页面横溢出；搜索、设置和账号的实际命中可用，搜索/设置关闭回焦、账号Escape、断点焦点、快速反向、顶部菜单切换和触屏44px命中已验。motion目录保存真实Edge开合录像；透明度动画读取到140ms，reduced-motion测试无活动动画。
- 初次light截图在主题140ms颜色过渡未完成时采样，保留light-transition.png；final-390-light.png在过渡完成后重新采样并目视确认图标可见。此项只调整采集时机，没有凭中间帧改动产品颜色。

## 八步自审

1. 完整性：收起搜索与导航、底部账号/设置、展开、顶部菜单共用状态已落地，后续连接/添加/生成不计入本轮完成。
2. 正确性：来源边界与内容组区别已核实，持久搜索DOM使关闭和视口切换可回焦。侧栏与画布工具的碰撞实际检查通过。
3. 健壮性：四视口、抽屉内搜索弹窗、外部点击、Escape、隐藏分组焦点、快速反向和触屏可执行。此次没有新增异步服务路径。
4. 结构：复用Sidebar、useDismissible与App原有导航；新增窄屏偏好hook与局部样式，组件均低于300行。
5. 性能：未加轮询/每帧测量；移除宽度插值。构建JS gzip115.63KB，未做新帧性能跑分，不能将构建体积等同交互性能。
6. 安全：不接API、不存密钥、不删除文件/数据、不写远端、不发布；测试使用隔离浏览器上下文，未刷新用户已有页面。
7. 验证证据：最终完整verify通过，并保留前后截图、首次失败与录像；不以74项通过代替设计验收。
8. 更好方案：保持原稿桌面偏置设置；窄屏同列排列解决真实遮挡。统一顶部/侧栏切换避免CSS强制隐藏造成假展开。尚无侧栏原型时序，不编造对应动画。

## 剩余与下一步

侧栏实时reactions/相邻状态、中文字体细节与完整像素验收仍待补齐；浅色、窄屏抽屉、粗指针命中及140ms透明度为工程补充。旧独立示范入口仍在，不能冒认正常生成流程已迁移。

下一小交付：先读实际账本和来源，核对图片/视频/结果可见卡片侧边中点，再修正磁吸小加号→按下拖出→释放后171:787添加菜单；然后迁移正常节点输入/生成/结果操作。原始提示词收藏入口也未补齐。仍有本地可执行内容，无需因当次Figma认证失败暂停整个安排。
