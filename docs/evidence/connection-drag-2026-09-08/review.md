# 连接拖出与添加菜单交付审查

2026-09-08，D:\kk-studio-next，codex/figma-new-pages / 9e20941。此单元Prototype，整体Repairing。演进为extend：修正已有连线/节点添加，不重建导航，不迁移生成流程。

1. 完整性：图片/两视频/结果卡片统一可见边界；加号磁吸、拖动预览、释放后菜单、选择后唯一创建/连线，普通点按/键盘与底栏/空白双击共用菜单。表格、3D、剪辑、ComfyUI未接入，明确禁用。
2. 正确性：图片源线中点偏差42→0px，50/100/200%检查通过；平移缩放后的释放点与新增卡片左侧中点重合，文案/音频源与终点亦居中。源171:787的外框/标题/八行/图标容器差0，视频徽标宽差0.09375px；10枚原始SVG SHA回验通过。
3. 健壮性：Escape、失焦、pointercancel、越界释放取消且不建节点；取消后Enter恢复。外部移焦关闭菜单，工具菜单互斥。触屏完整点按创建一次；滚动取消，不误建。输入草稿保留；视图与原节点不因拉线移动。
4. 结构：CanvasNodeFrame抽出原节点壳且使用相同id key，保留内容状态；cardVisualBounds和connectionGeometry共享几何，旧getConnectorPath委托同一来源。菜单通过portal复用，组件均在300行以内，无新增依赖/遗留基线扩张。
5. 性能：按住拖动时才注册全局取消监听，取消/释放即清理；磁吸与曲线仅更新当前NodeActions局部状态，没有整张画布拖动更新。build输出JS392.59kB、gzip117.52kB；未测1000节点帧率，不冒认性能预算验收。
6. 安全与真实状态：无网络请求、后端、服务生成或积分消费；保留用户草稿与所有历史工作。MiniMax H3是原稿徽标，按钮说明API未连接。只写前端、测试、原稿资产与文档；未提交/发布/修改Plate。
7. 验证：Node24.20.0最终npm run verify exit0，15核心+80 Edge，0失败/跳过/flaky；typecheck/build/format/ui:check66文件0违规。浏览器页面错误0。四视口、663×450短屏、浅色、reduced-motion及真实CDP触屏事件均有检查；final-*为最终截图，final-motion保留交互视频，source-compare.json为原稿几何/资产对比，164文件指纹保存。150个上轮指纹中仅13个既有源/测试文件发生本单元预期修改（changed-from-sidebar.json）。
8. 方案反思与边界：共享菜单避免入口继续分叉；可见卡身中心优于散落数字。焦点环/灰色禁用/触屏窗口适配/磁吸曲线/140ms是工程补充；菜单原稿reactions=[]及关键帧nodes=[]不推断其它模块无动效。源侧栏170:8486有ON_CLICK变体transition:null。中文回退字形、源底栏294与本地266、完整原型悬停、浅色缩小画布网格的密集观感仍有差异。没有宣称百分百还原或业务整体完成。

## 保留的失败与修正

- targeted-first.log：3过1失败，取消抑制误拦Enter；仅抑制鼠标兼容click，键盘不拦截。
- targeted-second.log：17过1失败，外部键盘移焦未关添加菜单；补focusin关闭。
- targeted-third.log：8过1失败，触屏拉线后菜单点按无兼容click；touch-debug*.json保存pointerdown/up但无click。touch-fourth/fifth.log的两次局部实验无效，相关实验代码已撤回，不重复全量检查。
- 最终菜单按完整触屏点按执行，位移>8px/取消不选择，正常click/键盘仍保留。targeted-final.log九项通过，然后执行一次全量verify通过。

## 后续

优先正常创建节点→下方输入→生成→本地真实演示结果及操作，移出独立示范库入口，仍需清晰注明演示/无服务消费；接着核对最新底栏294与更多参数/原型状态。初始图片/视频提示词收藏入口仍未补齐。本轮不改变自动化；仍有本地可执行事项。
