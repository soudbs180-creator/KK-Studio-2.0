# Spec

- ID: conversation-panel-2026-09-11
- Source of truth: Figma `410:67357` 的收纳态 HUD 与 `143:28077` 的 `右关闭` / `右打开` 组件；Workspace `404:28667` 用于展开态画布边界。
- 入口与状态: 关闭会话时显示 `打开对话`，按钮命中框为 `(1859,79,18,18)`；导航为 `(1562,72,281,31.109)`，地图触发器相对导航起点约 `206.61px`。打开后会话面板为 `(1430,61,470,998)`，导航保持可用并左移到 `(1138,72,281,31.109)`，地图触发器随导航左移到约 `x=1344.625`，与面板保留 10px 间距；底部工具栏仍为 `(719,998,294,50)`。
- 交互: 关闭态资源为 `chat-close.svg`，打开态资源为 `chat-open.svg`。面板通过绝对定位覆盖层承载，面板出现时以 `translateX(100%)` 到 `translateX(0)` 的单次进场动画滑入；`prefers-reduced-motion` 下禁用动画。
- 数据契约与权限: 仅改变展示层和已有 `onClose` / `onChat` 回调；消息、草稿和 Prototype 服务边界保持不变。
- 验收标准: 同一 1920×1080 状态下关闭态导航、地图与工具栏符合 Figma 坐标；打开态导航和地图整体左移并在面板左侧保留 10px 间距，工具栏仍为 x=719；面板采样从页面右侧移动到 x=1430，y 始终约 61；工作台 `scrollLeft` 保持 0；关闭态图标源与 Figma 关闭组件一致。
- 错误、取消、离线: 本次无网络请求或生成任务；已有会话空态与消息行为继续沿用。
