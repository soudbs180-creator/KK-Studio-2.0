# 实施计划
1. 核验 ROOT、依赖、旧完成记录，建立当前 dirty 源码的隔离候选与 SHA-256 基线；只回写本任务差异。
2. 先补失败测试：附件传输/身份/上限/取消与视口、选择操作和回传快照。
3. 实现图片准备服务、隔离草稿、画布引用入口、MCP → 真实画布桥；最小改动共享组件。
4. 运行相关单测和浏览器交互，完整 verify；重建桌面并执行独立临时数据目录的 Tauri 与真实 Codex 识图。
5. 独立上下文只读预检，修复问题；核验 ROOT 文件未被其他工作修改后回写、再验证并同步清单。
依赖顺序：图片准备 → 会话发送 → UI；画布桥可独立实现，最终统一运行验收。风险为迟到读取、原件失踪和控件状态/快照漂移。
回滚只恢复本任务写入前版本；不清理其他分支/文件/用户数据。
上游基准：basketikun/infinite-canvas e6d0911e9d509d00150eaab02f9ca05be94ffc46。沿用现有 MIT vendor 授权。
