# Codex/Agent 当前清单（2026-09-23）

沿用用户选择的最近 Codex/Agent 清单。TASK-AGENT-003 实现第一批附件与画布控制；TASK-AGENT-002 整体仍为 PARTIAL。测试与原工程最终整合状态以 [验证记录](verification.md) 为准；未发送 Agent 草稿目前只在页面会话中按项目保留，刷新后不恢复。

| 项目 | 当前状态 | infinite-canvas 复用与下一步 |
| --- | --- | --- |
| Windows Agent 一键启停、官方 Codex 会话 | 前轮完成；本轮继续通过随包执行器做原生识图与 MCP | 复用现有 Agent、进程归属、凭据隔离和同线程恢复；前轮 8/7 项完整生命周期记录不冒充本轮重跑 |
| Agent 本地图片附件、显式画布引用 | 第一批已实现；真实原件、失败保留、项目/API 隔离、限制与取消回归通过 | 复用 vendor attachments/localImage，接 KK 原件仓库；真实 Codex 识图有独立记录 |
| MCP 单选/多选/清空、移动缩放视口 | 第一批已实现；真实控件、快照、视口重开恢复通过 | 复用上游工具契约，KK scale 与上游 k 只在边界转换 |
| 内置生图参考图片编辑 | 仍待专项实现/真实验收 | 当前看图成功不代表编辑成功；接生成指令、参考归属与结果归档后单独验收 |
| Agent 站内项目/素材/提示词/任务查询 | 待接入，建议下一批 | 上游 agent-site-tools 可复用名称与 schema，执行改接 KK 仓库；先完成只读查询，再补有授权边界的写操作 |
| TTS/音频执行器 | 部分请求逻辑已移植，真实任务链未接通 | 接现有 requestAudioGeneration、任务/素材归档和原生宿主音频契约；不以演示状态当真实结果 |
| 视频执行器/首尾帧/恢复查询 | 未完成 | 复用上游 video.ts 的协议和轮询，接 KK journal、unknown/取消和受控下载 |
| Gemini 原生/其他非兼容 API、新模型参数 | 未逐协议完成 | 复用 image/video 协议、模型发现和能力映射；列到模型名不代表参数全部已知 |
| Proxy 一键托管 | 转发核心已有，仍手动启动 | 复用 Agent 生命周期前，先接认证、来源与目标限制 |
| Google AI Studio/Antigravity/豆包/WorkBuddy 登录适配 | 未完成 | 上游未提供这些产品的完整专用登录适配器；借用 Adapter 结构并核实各产品真实授权协议 |
| 后台网页/多窗口并发/登录失效恢复 | 未完成 | 上游多画布会话不等于浏览器会话池，需独立执行器与恢复逻辑 |
| 通用第三方 MCP/任意桌面 App | 未完成 | 本轮只完成 KK 画布工具；继续复用 KK MCP 握手与权限链，按具体服务验收 |
| 付费 Provider 逐厂商出图 | 未真实验收 | 受控服务测试和协议源码不能替代实际账号、额度与厂商结果 |

WebDAV 可复用现有传输层及上游 manifest/冲突编排，仍需适配 KK 原生素材和快照；它属于工程总清单，不因本批而完成。ComfyUI、账号/计费、长期记忆和正式安装发布同样保留原任务边界。

复用评估固定到上游 e6d0911e9d509d00150eaab02f9ca05be94ffc46，详见 [复用评估](reuse-assessment.md)。上述复用程度是源码审查结论，只有独立验证记录覆盖的能力才算已验收。
