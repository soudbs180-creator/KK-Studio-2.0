# 首页到工作台创作闭环

## 目标

让首页输入真正成为一个独立的本地项目：提示词、模型、参考图进入项目；工作台消息创建后续任务；项目库可以重新打开同一个项目；没有模型连接时显示可重试的失败状态，不伪造生成成功。

## 范围

本轮先覆盖图片创作、浏览器本地恢复和 OpenAI 兼容图片接口。界面沿用现有 Figma-first shell、输入栏和画布节点；账号、云端同步、视频/音频 provider 仍保持未接入状态。
