# 实施计划

1. 已完成：共享项目/任务/附件模型和本地快照；首页输入、素材、模板、模型选择接入创建项目。
2. 已完成：工作台消息、任务状态、真实 OpenAI 兼容 `images/generations` / `images/edits` 请求、结果节点回填和取消/重试。
3. 已完成：项目库按快照列出项目，点击项目回到对应工作台；画布为新节点生成独立位置。
4. 已完成本阶段：浏览器 IndexedDB 与 Tauri `%APPDATA%\\kk-studio` 项目快照写入边界；模型设置已接入既有系统凭据命令，浏览器保持会话内存，未把 API Key 放入项目快照。工作台草稿/附件恢复和中断任务人工重试也已补齐。后续再补真实 provider 能力元数据、视频任务和服务端任务恢复。

本轮不把浏览器本地快照描述为云端保存，也不把没有 API Key 的失败态描述为生成服务成功。
