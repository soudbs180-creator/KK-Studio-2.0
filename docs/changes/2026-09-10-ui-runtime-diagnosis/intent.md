# UI runtime diagnosis intent

Status: runtime delivery repaired; full-file visual acceptance remains open.

The user reports that the running page still looks like the old UI. Diagnose the actual runtime before visual edits. Preserve the existing dirty worktree, business behavior, local drafts and storage. Do not create another app or duplicate pages.

Authority: fresh Figma MCP reads of file 0nU0A7pq6eyjwfwm1TtWkO, Frames 404:28667, 410:67357 and 410:59708, plus page 0:1 for shared components.

Acceptance requires a real loaded browser page, same-state screenshots and DOM measurements, not only changed files or successful builds. Native Desktop acceptance is independent of Web acceptance.
