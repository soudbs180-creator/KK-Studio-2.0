# UI 架构核对（2026-09-25）

## 来源与运行态

- 用户新写的规范位于原工程未提交文件；本任务逐文件带入隔离 worktree，未覆盖原工程。入口是 `docs/UI_INDEX.md`。
- 当前可运行页面：本 worktree `npm run build` 后的 `http://127.0.0.1:1423/`（Vite preview）。截图与矩形在 `evidence/architecture/`；采集脚本 `evidence/capture-architecture.mjs`。用户浏览器的 `1421` 是另一 checkout，不能用于本分支验收。
- Ardot `728457371665311` 在线页面在此环境不可读取；其画布变量是否与新增 `tokens.css/json` 同步是 **UNKNOWN**。Figma 四节点已经在本任务先前的设计读取中核对，节点与尺寸见 `docs/DESIGN_TOKENS.md` 的专项来源表。

## 页面类型与本轮运行检查

| 入口 / 路由 | 类型 | 1440×900 | 390×844 | 观察 |
| --- | --- | --- | --- | --- |
| 开始创作 `landing` | A2 展示 + 创作输入 | 截图已采 | 截图已采 | 输入已改为紧凑形态；手机按触屏目标分两行，高于静态 Figma 外框 |
| 项目库 `projects` | A2 项目卡 + 空态 | 截图已采 | 截图已采 | 空项目不再冒充真实卡；侧栏仍存在静态演示项目，另行登记 |
| Skill `skills` | A2 卡片目录 | 截图已采 | 截图已采 | 目录卡已按 A2 专项收敛到 r12/gap16/padding12；大 Hero 卡继续取 r28 |
| ComfyUI `comfyui` | A2 卡片目录 | 截图已采 | 截图已采 | 与 Skill 共用 A2 目录卡样式；无真实 ComfyUI 连接，仍是预览 |
| 资产管理 `assets` | A1 画廊 + A4 详情 | 截图已采 | 截图已采 | 网格改为 min 180/110、gap12、r12/r8、文件名11/14；筛选28、搜索32；原6px文件名与22px筛选已移除 |
| 提示词库 `prompts` | A2 条目卡 + A4 预览 | 空态截图已采 | 空态截图已采 | 空态为240px单一加载入口；来源加载后条目按 A2 r12/padding12/标题与说明分层，右侧预览仅在有结果时出现 |
| 设置 `settings/*` | A4 详情面板 | 10 个分类均截图 | 同左 | 手机内容可滚动，顶部分组可横向滚动；未接通服务保留真实不可用说明 |
| 画布 `workspace` | A9 画布 | 收起/展开截图 | 收起/展开截图 | 桌面对话面板持久、HUD 无覆盖；手机底栏「对话」展开后点顶栏仍在，再点同一入口收起 |
| 任务列表 / 任务工作台 | A5 流程 + A3 任务矩阵 | 弹层/工作台截图已采 | 同左 | 空队列明确提示连接条件；当前仍是 Prototype，未接入真实任务服务 |
| 搜索 `search` | A3 结果列表弹层 | 截图已采 | 截图已采 | 分类底部分割线有移动指示；外框/输入/结果行已归入 r20/r10/r8 |

共采集 42 个页面状态（每个视口 21 个）；两组视口的 document 水平溢出均为 0。桌面与手机资产卡、筛选栏和内容区的计算矩形已有定向断言；这仍不能单独证明所有组件内部与可见控件都符合规范。

## 已确认的规范冲突

| 主题 | 互相冲突的现行来源 | 运行态 | 处理边界 |
| --- | --- | --- | --- |
| 页面外壳 | `tokens.css`：侧栏 200、顶栏 56；`UI_ARCHETYPES.md` §1：291、40；现有 Figma/运行态为 291、40 | 291、40 | 已向用户请求尺寸裁决；答复前保留经运行和 Figma 核对的外壳 |
| 目录卡 | `UI_ARCHETYPES.md` A2：r12、gap16、padding12；`DESIGN_TOKENS.md` §5–§7 与 `tokens.css`：通用大 card r28、卡片间距20、padding16 | 本轮 A2 专项 r12/gap16/padding12 | 通过类型专用规则区分标准目录卡与大 Hero 卡，未改动候选 token 导出 |
| 菜单项 | `DESIGN_TOKENS.md` §4 封闭高度轴不含 36；同文 §7 与 `tokens.css` 写 `menu-item-h:36`；`UI_RULES.md` §10.3 残留旧四档待决文案 | 菜单混用历史值 | 先修正过期文案；变量需画布重新导出，不伪称已完成 |
| 颜色 | `DESIGN-SYSTEM.md` 与 `global.css` 校正 dark secondary/tertiary 为 `#AAAAAA/#9E9E9E`；`tokens.css` 为 `#858585/#707070` | 消费 `global.css` | 保留较高对比度的现行颜色；不导入冲突颜色变量 |
| AI 角标前景 | 原 `UI_ARCHETYPES.md` / `archetypes.css` 把 `accent-soft` 底与近同色 `text-accent` 配对；运行 token 中 `accent-soft` 是浅色实体 | 资产角标使用 `text-on-accent-soft` | 已同步规则、JSON 与候选 archetype CSS，避免浅底浅字 |
| 接入状态 | `UI_INDEX.md` 称 `tokens.css` 为机器真源、`ui-tokens.css` 为兼容别名；`main.tsx` 实际只导入 `global.css`/`ui-tokens.css`，后者仍含完整数值 | 新架构文件未接线 | 标记为未完成迁移，禁止把“文件已存在”报告成“全站已按新体系运行” |

## 本轮独立可修的偏差

1. 搜索弹层的面板、输入与结果行已按面板/表单/列表语义归入 r20/r10/r8，分类指示线与键盘行为保留。
2. 手机对话已由真实底栏入口截图，展开后点击顶栏仍可见，再点入口收起。
3. 旧 UI 规范已变为只读指针；四节点尺寸只归 `DESIGN_TOKENS.md`，交互只归 `UI_RULES.md`，运行验证只归 `UI_SPEC.md`。
4. 资产管理的 A1 画廊和提示词库的 A2+A4 组合已按类型几何与空态调整；新增桌面/手机浏览器断言覆盖关键尺寸及操作。

## 尚未证明

- “全站完全符合新规范”尚未成立：新增 token/archetype 样式未接线，数值源互相冲突，在线 Ardot 未回读。现有自动门禁只检查部分颜色/变量/图标，不能证明全部控件档位、嵌套圆角或 A1–A9 状态完整。
- 上述 42 张截图是 1440 与 390 的默认状态审查。已有回归覆盖提示词库深浅主题、长文本、错误与加载，以及多视口基础操作；不等同真实服务全部状态与真实手机软键盘验收。新代码对应的 fresh Tauri release 已重建，原生加载的 JS/CSS 与本轮 production preview 指纹一致，详见 `verification.md`。
