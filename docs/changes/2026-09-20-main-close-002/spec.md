# Spec

- ID：TASK-MAIN-CLOSE-002
- 基线：main@c3ff0871b3db674e0ab073f1445879d84fee3507，tree 65328c59856eaf4d777698851664a33309a0faeb；开始时根目录干净且与 origin/main 一致。
- 入口与状态：页面子任务核对当前 Figma 与实际浏览器；性能子任务检查素材列表、预览与原件读取；治理审计子任务核对 PR #4、分支和保护规则。
- 数据与权限：原件哈希、导出和重绘必须保持一致；Desktop 原件仍使用原生资产仓库，Web 仍使用 IndexedDB；检查过程中不上传本地历史归档、运行数据或凭据。
- 验收标准：集成后的完整 verify 与相关 Rust 门禁通过；发生 UI 或素材运行行为改变时分别验收 dev1421、preview1423 和 Tauri release；PR 合并后回读本地/远端 main 的 SHA/tree 并核对产物。
- 自动回归补充：侧栏 CSS 动画通过实际 React 状态切换生成的 CSSTransition，在同一时间轴固定采样，保留起终点、中间位置、任务锚点、工具栏和文字显隐断言，不依赖点击之后的跨进程延迟。
- 未取得 Figma 节点、真实外部服务或服务器保护权限时，仅该对应验收项保留缺口，不把工程补充写成设计验收。
