# Intent：现行规则与 Markdown 一致性审计

- Task ID：TASK-RULES-004
- 状态：IMPLEMENTED，待当前 SHA 复审和 hosted 门禁
- 日期与提出者：2026-09-23，用户
- 请求来源：用户要求检查“全部的规则，是不是落实了”，尤其整理 Markdown；此前还要求多人开发可同步信息、功能落地并线并减少旧分支。
- 用户授权范围：审阅并整理现行规则、文档和相应检查脚本；历史证据与并行 dirty worktree 保留。
- 关联：[任务账本](../../governance/task-ledger.json)、[规范](spec.md)、[计划](plan.md)

## 用户原意与预期结果

新参与者开始工作时能找到同一套规则，知道谁在做什么、从哪条分支开始、何时同步及合并；文档描述必须与当前 2.1.0 候选和真实门禁一致，不能用写在 Markdown 里的口号代替执行。

本任务处理规则入口、当前事实、相对文件链接和分支流程。产品功能、安装包、GitHub 账户计费、旧分支清理及历史验证结果不属于本次改动。

## 验收条件

| ID   | 可观察结果                                           | 证明方式                                  |
| ---- | ---------------------------------------------------- | ----------------------------------------- |
| AC-1 | 现行规则入口使用一致的 Design System、任务和交付状态 | 对照设计规范、账本、Git/PR 与差异审计     |
| AC-2 | 上游 PR 延迟合入时，下游任务仍能协作且保留独立审查   | 分支规则说明堆叠 PR、合并后复验和清理边界 |
| AC-3 | 现行 Markdown 的本地文件链接错误会使 lint 失败       | 真实规则检查与缺失链接回归测试            |
| AC-4 | 规则已执行与未执行的部分清楚区分                     | 本任务 verification/review 和远端门禁回读 |

用户的“只保留一个主线”解释为唯一稳定集成线 `main`；短期任务分支、未合并工作和恢复证据仍需按状态保留。真正的远端保护和自动化不能从本地文档推断已生效。
