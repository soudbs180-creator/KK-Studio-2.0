# Review：规则与 Markdown 一致性审计

- Task ID：TASK-RULES-004
- 状态：两轮独立复审提出修改；第三轮独立补审 PASS；Hosted CI 仍阻断
- Base：`da811283e55ce699e4c5425d92ad31ffba7513e3`
- 首轮 Head：`6d550a6d7ab069e72cb29d1729c00d5bf8908370`；第二轮 Head：`ae142ded51411a63851010da1731dff2d795f50a`；第三轮 Head：`d8e12c0abe046f981901a0fcf59bc9210ccf98f2`；草稿 PR #10
- 范围：[intent](intent.md)、[spec](spec.md)、[plan](plan.md)、[verification](verification.md)、真实 Git diff 和可执行脚本

## 首轮独立复审与修复

独立上下文审阅了首轮已提交的完整 diff、相关规则、账本、PR #9 和测试，并以临时文件实际复现以下问题。首轮结论为 **CHANGES REQUIRED**；实现者随后加入先失败后通过的回归，并继续补审。

| ID  | 严重度/门禁      | 首轮发现                                                                                 | 修复与复验                                                                         |
| --- | ---------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| R1  | P2，阻断 AC-3    | `scripts/governance/markdown-links.mjs` 单行正则误报文件名中平衡括号，漏掉嵌套或跨行标签 | 先以逐字符解析修复并做 RED→GREEN 回归；第二轮后改为语法树解析                        |
| R2  | P2，阻断 AC-3    | 代码围栏关闭行含尾随文本时被错误视为关闭，随后代码示例被误检                             | 先收紧围栏关闭条件并做 RED→GREEN 回归；第二轮后由 Markdown 解析器处理围栏语义         |
| R3  | P3，既有记录遗漏 | REL-2.1.0 账本仍只写首次 15f1f27 上传、PR/CI 待验证                                      | 保留原验证时点并追加 da811 候选、PR #9 及 hosted 0-step 失败回读；重新生成账本视图 |

复审同时确认堆叠分支在上游 squash 后从新 main 建承接分支的规则合理、历史证据未改、交付包结构 27 文件/0 违规；PR #9 open/unmerged 且 job 失败 0 steps。首轮无其它本次引入的 P0/P1/P2。独立复审不是 GitHub 第二账号批准，也不覆盖 live 模型 eval、Desktop 原生测试或服务器保护生效检查。

## 第二轮独立复审与修复

第二轮针对 `ae142ded51411a63851010da1731dff2d795f50a` 的结论仍为 **CHANGES REQUIRED**。审查者独立运行前一轮的 RED→GREEN 回归，确认 R1/R2 原案例和 R3 均已解决，但发现新的 **R4（P2，阻断 AC-3）**：逐字符解析跳过嵌套图片，不能正确解码转义括号，把转义起始括号和缩进代码误认作链接，且把不同段落中的反引号错误配对，遮蔽真正的链接。

实现者新增覆盖这些情形和 GFM 表格的回归，先确认新增测试失败，再将扫描器改为 `remark-parse` + `remark-gfm` 的语法树遍历；相对路径检查保留。新增回归在工作树中通过，正式结论见第三轮补审。

## 第三轮独立补审

独立审查者针对 `d8e12c0abe046f981901a0fcf59bc9210ccf98f2` 的实际 diff 给出 **PASS**，确认 R1/R2/R3/R4 均关闭，无新的验收阻断。独立运行新回归 3/3、现行 Markdown 81 文件/0 违规、治理 61 任务/0 违规、精确 base/head 的 delivery 28 文件/0 违规，并对第二轮旧模块运行新回归得到 2 通过/1 失败，复核了 R4 的 RED→GREEN。额外临时输入中的百分号编码越界和非法百分号路径也被正确拒绝；`npm ci` 后的完整验证日志为 370 Node、300 browser。依赖锁文件新增 66 个条目，既有依赖版本和元数据未改；历史 `docs/archive` 与 `docs/evidence` 无本任务改动。此处 PASS 不等于 GitHub 第二账号批准或 Hosted CI 通过。

## 门禁状态

| 门禁            | 当前结果                                                                                                |
| --------------- | ------------------------------------------------------------------------------------------------------- |
| Self-review     | 本轮差异、干净工作树、旧证据保护和依赖锁文件已核对                                                       |
| 独立 AI review  | 首轮、第二轮均 CHANGES REQUIRED；第三轮针对 `d8e12c0` PASS，R1–R4 关闭                               |
| 本地 verify     | 370 Node、300 browser、治理 61/0、功能 29/0、Markdown 81/0、UI 159/0、类型/格式/Web build 通过         |
| Hosted CI       | PR #9 与草稿 PR #10 的 jobs 均因账户付款或 spending limit 在步骤前失败；未得到远端测试结果               |
| main 合并与发布 | 未执行；不可把本任务完成等同 2.1.0 发布                                                                 |

结论：本任务已提交范围的本地验证和独立 AI 补审为 PASS，PR #10 保持草稿，Hosted CI 与 main 合并仍为 PARTIAL。两轮失败审查保留在上文；后续只更新状态文档与账本时，还需对新增差异做定向检查。
