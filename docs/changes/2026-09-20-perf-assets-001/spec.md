# Spec

- ID：TASK-PERF-ASSETS-001
- Web 继续使用 IndexedDB 原始 Blob；新写入元数据不重复存完整 preview，旧记录可读取。Desktop 继续使用原生文件和 SHA-256。
- 元数据单页最多100条，UI每次40条；列表不解码全库原件。Rust旧 list() 保留完整校验，asset_list IPC改为页接口。
- 预览最长边320px、WebP、24项LRU，串行解码；离开可见区/卸载取消等待，过期队列不再读原件。
- 搜索/筛选自动补齐剩余元数据页，不因未加载页误报空库。原图读取失败有明确错误和重试，不显示成功。
- 原件用于详情、重绘与导出；预览缓存不得作为原件。新加载更多和错误状态为工程补充，不冒充现行Figma定义。
