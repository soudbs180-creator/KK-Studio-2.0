# UI 与新增功能衔接

用户要求继续未完成事项，重点解决功能已增加但 UI 没有落实、界面与功能不匹配的问题。用户请求授权范围内的 AI 技术决策：按现有 Design System 1.1 修复入口、能力状态及交互，保持既有布局和数据身份，不提交或推送。

成功结果：用户能明确选择图片任务或本地 Agent；看到实际插件状态并进入正确管理页；从提示词库选用文本到草稿；各平台、异步失败和尚未接通功能如实显示。
