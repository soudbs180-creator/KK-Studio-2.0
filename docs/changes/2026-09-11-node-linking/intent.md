# 节点连线与生成卡片交互

## 用户意图

让画布中的加号连接符合拖放直觉：拖到卡片时直接连接并高亮目标，拖到空白时才打开新增菜单；生成多张图片时先在右侧建立等待中的结果卡片，并让每张结果继续保留自己的提示词输入。

## 边界

- 只改画布节点、连接、图片/视频生成编辑器和本地演示状态。
- 参考图数量由当前卡片的本地模型能力元数据限制；真实 provider 接入后由 provider 元数据替换。
- 本地结果仍标记为演示素材，不宣称已连接真实模型或服务。
