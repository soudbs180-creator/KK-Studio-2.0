# 交互规格

## Figma 依据

- 文件：`0nU0A7pq6eyjwfwm1TtWkO`
- 节点：`410:67353`（卡片展示）
- 最新元数据显示创建卡片为 `436×458`，图片正文为 `400×400`，视频正文为 `370×208`，输入区为 `590×237`；`aspect-[441/441]` 是占位内容比例值，不是卡片宽度。

## 状态规则

1. 加号拖动超过 4px 后显示连接预览线；指针落在其它卡片的可见正文（图片、视频或结果卡）时目标卡片高亮，释放直接建立连线。
2. 释放在画布空白区域时打开带 `parentId` 的新增菜单；菜单选择后自动把新卡片接到父卡片。
3. 每个媒体模型拥有 `maxReferences`；图片模型默认 `kk Image 2=4`、Pro=8、Lite=2、Image 1=1，视频 `MiniMax H3=1`。超限连接保留卡片但拒绝连线并给出状态提示。
4. 顶部上传生成的是 `referenceOnly` 图片卡片，选中不挂载底部 composer；composer 内的“添加参考图片”生成独立参考图卡片并连接到当前媒体模块，缩略图上的移除按钮可以直接断开对应连接。
5. 生成数量为 4 时先保留 4 个 `pending` 结果节点，演示素材返回后按索引填充，提示词和模型字段沿父模块复制；生成结果选中后继续显示跟随该模块的输入区。
