# Spec

- ID：2026-09-11-runtime-tasks
- Source of truth：Figma 文件 `0nU0A7pq6eyjwfwm1TtWkO`，Runtime / Tasks `396:938`，标题 `398:25710`、筛选 `398:25690`、任务区 `398:25593`。已读取设计上下文、节点元数据和导出图像。
- 入口与状态：工作台的“任务列表”按钮打开面板；默认“全部”显示历史任务与“正在生成中”两张卡。执行中和未完成显示运行卡，失败显示空态。点击卡片进入本地详情，关闭详情返回列表；入口关闭、外部点击和 Escape 都清理详情状态。
- 数据契约与权限：两张卡片是显式本地 Prototype fixture，使用原稿日期、耗时和占位图。卡片提示与详情说明不上传或保存到服务器；没有网络调用、密钥、账号或任务持久化变化。
- 验收标准：面板 `196×240`；面板内标题 `(14,11,169,10)`，筛选 `(14,30,170,14)`，卡片区 `(14,53,170,52)`；卡片 `40.25×52`，缩略图 `40.25×46`。同状态浏览器截图、DOM 几何、computed style、资源加载和实际筛选/关闭行为均有证据。
- 错误、取消、离线：这是固定本地快照，没有异步服务状态。失败筛选展示“暂无失败任务”；关闭即取消本地详情查看。网络离线不影响本地资源与交互，不显示真实生成成功。
- 工程补充：卡片详情视图、失败空态、焦点/透明命中范围及浅色主题颜色采用现有语义令牌；这些状态不作为 Figma 原稿动效或服务行为的证明。
