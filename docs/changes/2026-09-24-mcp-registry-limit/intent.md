# Intent：MCP 服务器配置上限与竞品审计遗留

- Task ID：TASK-MINIMAX-001
- 状态：IMPLEMENTED（本次子项）；整体任务仍 PARTIAL
- 日期与提出者：2026-09-24，用户要求继续解决未完成任务并检查遗漏、bug 和冲突
- 用户授权范围：对照本机 MiniMax Design 证据，修复 KK Studio 已确认的 MCP 数据缺陷，登记超出本次小修复的协议缺口。

## 用户原意与工程目标

用户希望继续处理竞品对标后的未完成项，并检查遗漏、错误和冲突。当前 MCP 设置允许添加第 51 个服务器，但持久化读取上限为 50；这会使下次启动把整份列表视为损坏。本次保证写入不会产生自己无法读取的数据，并把新版协议兼容性列为独立待办。

## 范围与验收

| ID | 用户可观察结果 | 证据 |
| --- | --- | --- |
| AC-1 | 已保存 50 个服务器时，第 51 个被明确拒绝，原 50 个仍可重启读取 | 单测先失败后通过；存储字节不变 |
| AC-2 | 已有服务器在达到上限后仍可更新 | 单测读取新 registry |
| AC-3 | 2026-07-28 MCP 兼容缺口有独立任务，不把当前旧协议实现称为通用支持 | 代码、官方协议文档、账本与功能卡 |

本次不改 UI 布局、凭据策略、协议握手或第三方服务授权。50 个上限来自现有持久化 schema；保留该上限是最小兼容修复。真实第三方服务器、MiniMax 付费生成和 Tauri release 仍未验证。
