# KK Studio UI 系统审计意图

- ID：`2026-09-16-ui-system-audit`
- 状态：Implemented（只读审计与规范文档已完成）
- Figma URL/node：`0nU0A7pq6eyjwfwm1TtWkO`；`404:28667`、`410:67357`、`410:67353`、`458:948`、`458:972`、`312:2436`
- 用户问题：AI 延展后出现组件漂移、动态文本固定宽度、信息层级混乱和响应式溢出。
- 预期结果：形成当前审计、漂移优先级、source of truth、设计系统、布局规则、IA、AI 护栏和七阶段计划。
- 不在范围内：本轮不改动 `src/` UI、业务逻辑、后端、provider 或真实服务边界。

## 目标

在不改动 `src/` UI 实现的前提下，建立一份可复核的当前 UI 审计，回答：

- 当前页面、组件、样式和运行链路是什么；
- 当前 Figma 与运行态之间的可确认关系是什么；
- 哪些问题属于系统性设计债务，哪些只是页面级偏差；
- 下一轮应如何收敛 Design System、布局约束、信息架构和 AI 生成 UI 的护栏。

## 范围

本次覆盖 Landing、Workspace、收纳态、资产面板、任务面板与任务工作台、设置与模型供应商、账号弹层，以及 1440px 和 390px 两种浏览器尺寸。审计证据来自本轮固定端口 `1421` 的 Vite 运行态、Playwright 截图与 DOM/样式采样，并与当前 Figma 文件 `0nU0A7pq6eyjwfwm1TtWkO` 的可访问节点对照。

本次不修改 `src/`、`public/` 或业务逻辑，不把本地 Prototype 任务、账号、积分、生成和保存状态描述为真实服务。

## 验收口径

审计结论必须能回到当前证据：截图、DOM/几何采样、计算样式、源代码位置或 Figma 节点。历史截图和历史聊天结论不作为本轮的同状态证据。Landing 的旧节点 `410:59708` 当前无法读取，因此 Landing 的 Figma 基线需要在后续实现前重新建立。
