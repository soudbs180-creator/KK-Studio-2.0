# Review

## 自审

- [ ] 当前候选分支的最终 diff 已逐文件检查。
- [ ] 所有新增可见控件都有行为或明确 disabled/Prototype 原因。
- [ ] Skill 导入、持久化损坏、指令长度和 MCP 会话释放有针对性测试。
- [ ] `npm run verify` 等价检查和固定端口浏览器证据已记录。

## 独立审阅

旧的静态审阅针对基线 `b637302`，发现的 Skill/MCP 风险已在本轮候选修改中处理一部分；候选分支新增的连接器目录、焦点恢复和流式大小限制需要以最终 HEAD 重新审阅。没有把旧审阅当作最终 HEAD 的 PASS 证据。

## 未关闭范围

- 真实第三方连接器安装和本机插件协议未接入。
- MiniMax 会员生成、云端账号/积分/provider 和付费下载未执行。
- Tauri `tauri.localhost` release 未在本轮重新构建和验证。
- Skill 编辑器没有完整焦点陷阱；当前覆盖 Escape 与关闭后的焦点恢复。

因此本任务的审阅结论保持 `PARTIAL`，不能宣传为 MiniMax 的完整产品替代品。
