# Spec

- ID：desktop-data-stability-20260916
- Source of truth：本轮用户指令、AGENTS.md、审计 plan.md 的 T1/T2。
- 读取状态必须区分首次空目录、成功、有效备份恢复和错误（损坏/未来版本/权限/冲突）。失败禁止自动写入与“重试保存”；重试只能重新读取，内存草稿可独立下载。
- 仅在权威读取成功后开启写入。保存串行、防抖；迟到的旧保存不得把新修改标成已保存；发生 revision 冲突后冻结自动保存，保留内存草稿。
- Desktop 文件读写保留至少一个已知有效副本和迁移前原件，完整有效性检查先于写入；比较预期 revision，临时文件 sync 后提交；失败不破坏原件。
- Web 读取不能将损坏/未来版本或不可访问的持久源当成首次空启动；IDB 保存事务校验 revision，保留恢复副本。
- CreationSnapshot 继续使用 version=2；新增项目 canvas.version=1，包含 positions、edges、viewport。没有 canvas 的既有 2.0 快照通过明确的默认布局升级，原始持久副本保留。未知 graph 版本或有歧义的重复身份不能静默丢数据。
- 新节点身份使用随机 UUID（保留 added-kind 前缀供既有交互）；所有图变化绑定项目 ID，切换时不串项目。无项目示例画布保留现有交互，不伪称保存为项目。
- 验收：读失败点击重试/编辑/刷新不发空写；真实文件测试覆盖损坏、备份、未来版本、权限/写盘故障与冲突；A/B 项目移动/连线/缩放/添加/删除后切换、刷新、Desktop 重开仍对应；测试仅使用隔离的合成数据。
