# TASK-DS-001 · Design System 校正与对齐

用户要求先检查新制定的 Design System，尤其是颜色和基础组件；发现错误直接修正，然后让后续 UI 和已有 UI Markdown 规范、公共实现向该系统对齐。此前只读审计限制已由这一明确修改请求更新。本次属于用户请求授权范围内的 AI 技术决策。

来源：用户提供 Ardot 文件 `728457371665311` 的 `0:1` 页面，以及同名 7 页 PDF。以完整 PDF 作为本次可读取的设计快照，不把其中“已与代码一致”“满足 AA”等文字当作已验证事实。

范围：设计系统规范、双主题语义颜色、8 种强调色、基础控件及其现有消费点、相关文档入口和自动校验。沿用 Inter、暗色工作台、原有页面几何与业务交互。保留用户数据身份、旧设置兼容性和无关并发修改。Agent 协议、安全、Provider 等上一轮审计问题不由本次样式任务接管。

交付：可追溯的设计问题清单、校正版设计系统、公共 UI 实现、真实浏览器证据及未验收项。原始 PDF 保留；在线 Ardot 没有成功写入就不能报告为已更新。
