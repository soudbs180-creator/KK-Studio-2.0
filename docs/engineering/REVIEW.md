# 评审规则

评审按三轮进行：

1. 行为：按钮、跳转、取消、错误和离线状态是否真实可用。
2. 视觉：Figma 同状态下的布局、间距、图标、颜色、动效和响应式是否一致。
3. 工程：类型、schema、密钥边界、迁移回滚、测试和文档是否完整。

高风险变更（凭据、导入、删除、外部模型路径）必须提供失败路径和恢复证据。硬编码账号、积分、版本或生成结果只能标注 Prototype，不得当作服务成功。
